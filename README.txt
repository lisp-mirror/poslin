This is an implementation of poslin.

Language documentation is currently not available. For reference use
the old tutorials in previous commits. For available primary
operations, look into source/prims/prim.lisp

The REPL is started via the REPL0 function.
There is a function REPL1 which would load the standard library, but
the current standard library is currently also not available.
